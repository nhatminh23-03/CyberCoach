[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    integration: Exercises FastAPI endpoints or multi-service internal flows.
    live_model: Optional tests that call configured external model providers.
